{
  "name": "generator",
  "version": "0.0.0",
  "scripts": {
    "tsc": "tsc",
    "start": "tsc && node dist/index.js  && prettier ./build/**/*.ts --write",
    "generate": "tsc && node dist/index.js && prettier ../src/modules/**/*.ts --write"
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "prettier": "^2.1.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.5"
  }
}
